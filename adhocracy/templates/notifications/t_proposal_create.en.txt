${e.user.name} has created a new proposal, ${e.proposal.label} within the issue ${e.proposal.issue.label}.  

Here's the inital description: 
${e.proposal.comment.latest.text}

Have a look at the proposal page: ${h.instance_url(e.proposal.instance, path="/proposal/%s" % e.proposal.id)}